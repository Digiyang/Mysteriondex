



#define _2_PI 3.14 + 3.14 // Makros müssen mit einem Buchstabe anfangen,sonst müssen wir _ am Anfang hinzufügen .



#define MULT(a, b) (a * b)		// ohne Klammern 
#define MULT2(a, b) ((a) * (b))		// mit Klammmern 

#define ADD(a, b) (a) + (b)
#define ADD2(a, b) ((a) + (b))
